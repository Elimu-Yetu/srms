# Deny ALL direct web access to this directory.
# All database files, logs, and backups here must NOT be served by Apache.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
